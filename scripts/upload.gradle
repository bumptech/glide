apply from: "${rootProject.projectDir}/scripts/artifacts.gradle"
if (project.hasProperty('BINTRAY_UPLOAD') && BINTRAY_UPLOAD.toBoolean()) {
    apply from: "${rootProject.projectDir}/scripts/bintray.gradle"
} else {
    apply from: "${rootProject.projectDir}/scripts/maven_central.gradle"
}
