# Cap Robolectric target SDK to 34 because the active Robolectric 4.11.1 version
# in this project only supports simulation up to Android SDK 34 (maxSdkVersion=34).
# Using targetSdkVersion 35/36 causes sandbox initialization failures and GHA CI network hangs.
sdk=34
